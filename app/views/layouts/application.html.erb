<!DOCTYPE html>
<html>
<head>
  <title>Hill30StandupBot</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="//fonts.googleapis.com/icon?family=Material+Icons">
  <link rel="stylesheet" href="//code.getmdl.io/1.1.3/material.indigo-pink.min.css">
  <script defer src="//code.getmdl.io/1.1.3/material.min.js"></script>


  <script src="//code.angularjs.org/1.2.0-rc.3/angular.js"></script>
  <script src="//code.angularjs.org/1.2.0-rc.3/angular-resource.js"></script>

<!-- Wide card with share menu button -->
<style>
  .demo-card-wide.mdl-card {
    width: 512px;
  }
  .demo-card-wide > .mdl-card__title {
    color: #fff;
    height: 176px;
    background: url('https://getmdl.io/assets/demos/welcome_card.jpg') center / cover;
  }
  .demo-card-wide > .mdl-card__menu {
    color: #fff;
  }
</style>

  <%= csrf_meta_tags %>
</head>
<body ng-app="app" ng-controller="reportController">
  <%= yield %>
</body>
</html>

<script>
  angular.module('app', ['ngResource'])
    .controller('reportController', ['$scope', '$resource', function($scope, $resource) {

      /*
        var currentDate =

        var getReport = $resource('/report', {}, {
          submit: { method:'POST' }
        });

        $scope.submit = function(payload) {
          _payload = angular.copy(payload);
          terminal_notification.submit(_payload, function() {
          }, function(res) {
            console.log(res);
          }, function(res) {
            alert(res.data.message);
          });
        };
      */

    }]);
</script>
